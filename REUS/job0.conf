source remd.conf

# prevent VMD from reading umbrella.namd by trying command only NAMD has
if { ! [catch numPes] } { source ./umbrella-remd.namd }
##if { ! [catch numPes] } { source ../umbrella-mod.namd }

